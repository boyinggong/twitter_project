source("./read_data.R")
source("./bagofwords.R")
data.person <- combined_timelines %>% filter(PERSON_FLAG == "1", abs(tweet_time_lag) < 20)
post   <- JH_word_of_bag_POST(data.person)
during <- JH_word_of_bag_DURING(data.person)
pre    <- JH_word_of_bag_PRE(data.person)
names(pre) <- c("rank", "PRE\n\nMALE", "PRE\n\nMALE COUNT", "PRE\n\nFEMALE", "PRE\n\nFEMALE COUNT")
names(during) <- c("rank", "DURING\n\nMALE", "DURING\n\nMALE COUNT", "DURING\n\nFEMALE", "DURING\n\nFEMALE COUNT")
names(post) <- c("rank", "POST\n\nMALE", "POST\n\nMALE COUNT", "POST\n\nFEMALE", "POST\n\nFEMALE COUNT")
# View(pre)
# View(during[,-1])
# View(post)
# View(cbind(pre, during[,-1], post[,-1]))
library(knitr)
View(cbind(pre, during[,-1], post[,-1]))
